{
  "name": "anualprint",
  "version": "1.0.0",
  "private": true,
  "description": "AnualPrint - aplicación financiera web/PWA preparada para Android con Capacitor.",
  "scripts": {
    "build": "node scripts/prepare-capacitor-web.js",
    "cap:sync": "npm run build && npx cap sync android",
    "cap:open": "npm run build && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/core": "^7.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.2.0"
  }
}
